{{#if show}}
<div class="pills-group empty sw5e-starship-sidebar-max-fires" aria-label="{{ariaLabel}}">
  <h3 class="icon">
    <i class="fa-solid fa-crosshairs fa-fw" inert></i>
    <span class="roboto-upper">{{label}}</span>
    <span class="counter">{{value}}</span>
  </h3>
</div>
{{/if}}
